apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: inno-microservices
  labels:
    app: api-gateway
spec:
  type: ClusterIP
  selector:
    app: api-gateway
  ports:
    - name: api-gateway
      port: 8080
      targetPort: 8080
      protocol: TCP