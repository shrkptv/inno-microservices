apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: inno-microservices
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: user-service
      port: 8081
      targetPort: 8081
      protocol: TCP