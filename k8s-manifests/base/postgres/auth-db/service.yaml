apiVersion: v1
kind: Service
metadata:
  name: auth-db
  namespace: inno-microservices
  labels:
    app: auth-db
spec:
  selector:
    app: auth-db
  ports:
   - port: 5432
     name: postgres
  clusterIP: None