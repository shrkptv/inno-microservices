apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: inno-microservices
  labels:
    app: user-db
spec:
  selector:
    app: user-db
  ports:
   - port: 5432
     name: postgres
  clusterIP: None