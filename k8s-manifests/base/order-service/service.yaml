apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: inno-microservices
  labels:
    app: order-service
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - name: order-service
      port: 8082
      targetPort: 8082
      protocol: TCP