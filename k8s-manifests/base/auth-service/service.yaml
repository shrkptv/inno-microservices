apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: inno-microservices
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
    - name: auth-service
      port: 8000
      targetPort: 8000
      protocol: TCP